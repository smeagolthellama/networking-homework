project Default is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("client.adb", "server.adb");

   package Compiler is
      for Switches ("ada") use ("-g", "-gnata", "-gnatf", "-gnat12", "-gnatVa", "-gnatw.e");
   end Compiler;

end Default;

